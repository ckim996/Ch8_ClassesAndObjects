public class Area
{

    // Circle
    public static double getArea(double r)
    {
        return Math.PI*
    }

    // Rectangle
    public Area(double l, double w)
    {
        length = l;
        width = w;
    }

    // Cylinder
    public Area(double r, double h)
    {
        radius = r;
        height = h;
    }
}
